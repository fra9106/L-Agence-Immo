{% extends 'base.html.twig' %}

{% block title %}Gerer les affaires{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Nos affaires :</h1>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for property in properties %}
                <tr>
                    <td>{{property.title}}</td>
                    <td>
                        <a href="{{ path('admin.property.edit', {id: property.id}) }}" class="btn btn-success">Éditer</a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>


    </div>


{% endblock %}
